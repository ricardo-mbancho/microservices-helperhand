name: Deploy API Gateway

on:

  push:

    branches:

      - master
jobs:

 build:

  runs-on: ubuntu-latest

  steps:

  - uses: actions/checkout@v4

  - name: Docker login

    uses: docker/login-action@v3

    with:

      username: ${{ secrets.DOCKER_USERNAME }}

      password: ${{ secrets.DOCKER_PASSWORD }}

  - name: Build image

    run:

      docker build -t ${{ secrets.DOCKER_USERNAME }}/api-gateway:${{ github.sha }}  ./microservices/api-gateway

  - name: Push image

    run:

      docker push ${{ secrets.DOCKER_USERNAME }}/api-gateway:${{ github.sha }}

 deploy:

  needs: build

  runs-on: ubuntu-latest

  steps:

  - uses: actions/checkout@v4

  # - name: Helm deploy

  #   run: |

  #     helm upgrade --install api-gateway \
  #     ./helm/api-gateway \
  #     --set image.tag=${{ github.sha }}